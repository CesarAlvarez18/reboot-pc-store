# Dependencias del backend de Reboot PC Store.

Django~=5.1.0
djangorestframework~=3.15.0
psycopg[binary]~=3.2.0
django-environ~=0.11.2
gunicorn~=23.0.0
whitenoise~=6.8.0

# Pillow lo exige ImageField de Django, así que entra junto con el modelo de fotos
# y no en la tarea 4 como decía el plan.
Pillow~=11.0.0

# Almacenamiento de las fotos en Cloudflare R2, que habla el protocolo de S3.
django-storages[s3]~=1.14.4
