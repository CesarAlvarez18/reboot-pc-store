# Dependencias del backend de Reboot PC Store.
# Las de almacenamiento de fotos (django-storages, boto3, Pillow) entran en la
# tarea 4 del plan; acá va solo lo que necesita el esqueleto y el despliegue.

Django~=5.1.0
djangorestframework~=3.15.0
psycopg[binary]~=3.2.0
django-environ~=0.11.2
gunicorn~=23.0.0
whitenoise~=6.8.0
